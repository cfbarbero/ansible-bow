# install packages
- name: install packages
  apt: name={{ item }} state=installed
  with_items:
    - ansible
    - git
    - git-review
    - graphviz
    - lsof
    - pylint
    - vagrant
    - python-pip
    - maven